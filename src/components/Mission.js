import React from 'react';
import styled from 'styled-components';

// Keep notes from extending wider than 800px
const StyledMission = styled.article`
  
`;


const Mission = ({ mission }) => {
  return (
    <div>
      
      <p>hellohhh</p>
    </div>
  );
};

export default Mission;